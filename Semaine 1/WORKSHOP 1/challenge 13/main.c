#include <stdio.h>
#include <stdlib.h>

int main()
{
    int number;
    printf("entrer un nombre entier:");
    scanf("%d",&number);
    printf("votre nombre en octale est %o et votre nombre on hexadicimal est %x",number);

    return 0;
}
